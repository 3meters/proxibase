#!/bin/sh

# kill all running node.*prox procs on the box
# Unix / Linux only 

# get the process Ids of running node processes
PIDS=`nodelook | awk '{print $1}'`

# if there are any kill them
if [ "$PIDS" ]; then
  kill -9 $PIDS
fi

