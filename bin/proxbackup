#!/bin/bash

# proxibase backup script

backupdir="/var/backups/prox"
mailto="backup@3meters.com"
cmd="mongodump"

today=`date`
$cmd --db prox --out $backupdir/dump
tar czf $backupdir/proxdump.gz $backupdir/dump
uuencode $backupdir/proxdump.gz | mailx -s "Proxibase backup $today" $mailto


