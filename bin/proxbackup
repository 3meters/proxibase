#!/bin/bash

# proxibase backup script

backupdir="/var/backups/prox"
mailto="backup@3meters.com"

today=`date`
mongodump --db prox --out $backupdir/dump
tar czf $backupdir/proxdump.gz $backupdir/dump
uuencode $backupdir/proxdump.gz proxdump.gz | mailx -s "Proxibase backup $today" $mailto


