#!/bin/sh
#
# dispaly the proxibase log or error log
#

if [ ! $PROXLOG ]; then
  PROXLOG='/var/log/prox/prox.log'
fi

if [ ! $PROXERRLOG ]; then
  PROXERRLOG='/var/log/prox/proxerr.log'
fi

if [ "$1" = "-e" ]; then
  cat $PROXERRLOG
else
  cat $PROXLOG
fi
