/**
 * Redirector for proxibase utility modules to enable
 *
 *   var proxdb = require('proxdb')
 *
 *
 * without knowing the relative path to the proxibase db folder.
 */

module.exports = require('../lib/db')
