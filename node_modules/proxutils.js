/**
 * Redirector for proxibase utility modules to enable
 *
 *   var util = require('utils')
 *
 * without knowing the relative path to the proxibase utils folder.
 */

module.exports = require('../lib/utils')
