/**
 * Redirector for proxibase utility modules to enable
 *
 *   var util = require('proxutils')
 *
 * without knowing the relative path to the proxutils folder.
 */

module.exports = require('../lib/proxutils')
